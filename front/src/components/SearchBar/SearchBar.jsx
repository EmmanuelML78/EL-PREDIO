import React from "react";

// import s from "./SearchBar.module.css";
// import styled from "styled-components";

const SearchBar = (props) => {
	return (
		<>
			<h1>Este Es El Componente SearchBar</h1>
		</>
	);
};

export default SearchBar;
